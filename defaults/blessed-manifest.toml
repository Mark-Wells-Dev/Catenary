# The blessed-manifest — the committed record of servers that have PASSED the
# CI conformance gate. This is the ONLY recipe-derived data a user surface is
# ever allowed to consult (the suggestion/fix-it surface ships later, in
# tickets 03/06); recipe DRAFTS (defaults/recipes.toml) never appear here.
#
# Format — one `[blessed.<server>.<platform>]` per conformed (server, platform),
# keyed by the same canonical server name as `defaults/servers.toml` and a
# platform token (`linux-x86_64`, `macos-arm64`). Rows are platform-qualified
# (misc 164): the same server list blesses on both platforms, so a server carries
# one row per platform it conformed on, and this ONE committed manifest holds
# both — there is no per-platform manifest file.
#
#   [blessed.<server>.<platform>]
#   version  = "1.2.3"            # the exact version that conformed
#   platform = "linux-x86_64"     # where the conformance job ran (= the row key)
#   date     = "2026-07-07"       # ISO date the green job landed
#   tier     = "verified-on-linux"  # honesty tier (initial tier; expands on
#                                    # sightings, decision-030 style)
#
# UPDATE FLOW (no auto-push — the human blesses by merging):
#   1. A conformance matrix job (.github/workflows/conformance.yml) installs the
#      pinned server and runs the harness with CATENARY_CONFORMANCE=<server>.
#   2. On green, the job emits its `[blessed.<server>]` entry as a build
#      artifact; an aggregate step combines the per-server entries and uploads
#      the merged manifest as a PR artifact and prints it in the job summary.
#   3. A human reviews the refresh-recipes PR (new pins + their conformance
#      results together) and commits the updated manifest into this file. Nothing
#      is pushed automatically — blessing IS that human merge.
#
# First blessing: 2026-07-08, from workflow_dispatch run 28948357654 (the
# matrix's first green subset — 13 servers). Recipe-backed entries become
# offerable via BlessedRecipe (manifest+recipe version match); provisioned
# entries (clangd, lattice, lua-language-server, rust-analyzer) record config-conformance
# only and are structurally unofferable (no recipe). Host-riding provisions
# carry their verbatim `--version` line — honest, if verbose.
#
# Full-matrix blessing: 2026-07-09, from run 28987704409 at 87ff482 — 18/18
# green, the entire matrix post-sql-ls drop. First run where every server
# conformed: elm-language-server on a cold ELM_HOME (ungrandfathered), gopls
# with diagnosticsDelay enforced-absent, ruby-lsp with GEM_HOME provisioned.
#
# Platform-qualified rows (misc 164, 2026-07-10): the row key gained a platform
# segment (`[blessed.<server>.<platform>]`) so the macOS leg's `macos-arm64`
# rows land in this same file alongside the Linux `linux-x86_64` rows. The 18
# rows below are the mechanical migration of the full-matrix blessing above —
# every field unchanged, the header re-qualified. macOS rows are appended by
# committing the macos bless job's candidate entries.

[blessed.ansible-language-server.linux-x86_64]
version = "26.6.0"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.bash-language-server.linux-x86_64]
version = "5.6.0"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.clangd.linux-x86_64]
version = "Ubuntu clangd version 18.1.3 (1ubuntu1)"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.docker-langserver.linux-x86_64]
version = "0.15.0"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.elm-language-server.linux-x86_64]
version = "2.8.0"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.gopls.linux-x86_64]
version = "v0.22.0"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.intelephense.linux-x86_64]
version = "1.18.5"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.lattice.linux-x86_64]
version = "0.4.0"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.lua-language-server.linux-x86_64]
version = "3.18.2"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.pyright-langserver.linux-x86_64]
version = "1.1.411"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.ruby-lsp.linux-x86_64]
version = "0.26.10"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.rust-analyzer.linux-x86_64]
version = "rust-analyzer 1.95.0 (5980761 2026-04-14)"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.svelteserver.linux-x86_64]
version = "0.18.3"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.taplo.linux-x86_64]
version = "0.10.0"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.vscode-css-language-server.linux-x86_64]
version = "4.10.0"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.vscode-html-language-server.linux-x86_64]
version = "4.10.0"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.vscode-json-language-server.linux-x86_64]
version = "4.10.0"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.typescript-language-server.linux-x86_64]
version = "5.1.3"
platform = "linux-x86_64"
date = "2026-07-13"
tier = "verified-on-linux"

[blessed.yaml-language-server.linux-x86_64]
version = "1.23.0"
platform = "linux-x86_64"
date = "2026-07-09"
tier = "verified-on-linux"

[blessed.ansible-language-server.macos-arm64]
version = "26.6.0"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.bash-language-server.macos-arm64]
version = "5.6.0"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.clangd.macos-arm64]
version = "22.1.8"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.docker-langserver.macos-arm64]
version = "0.15.0"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.elm-language-server.macos-arm64]
version = "2.8.0"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.gopls.macos-arm64]
version = "0.23.0"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.intelephense.macos-arm64]
version = "1.18.5"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.lattice.macos-arm64]
version = "0.4.0"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.lua-language-server.macos-arm64]
version = "3.18.2"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.pyright-langserver.macos-arm64]
version = "1.1.411"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.ruby-lsp.macos-arm64]
version = "0.26.10"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.rust-analyzer.macos-arm64]
version = "2026-07-06"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.svelteserver.macos-arm64]
version = "0.18.3"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.taplo.macos-arm64]
version = "0.10.0"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.vscode-css-language-server.macos-arm64]
version = "4.10.0"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.vscode-html-language-server.macos-arm64]
version = "4.10.0"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.vscode-json-language-server.macos-arm64]
version = "4.10.0"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

[blessed.yaml-language-server.macos-arm64]
version = "1.24.0"
platform = "macos-arm64"
date = "2026-07-10"
tier = "verified-on-macos"

# ── Publisher-discipline metadata (diagnostics-debt 04) ──────────────────────
#
# One `[discipline.<server>]` per blessed server whose adapter needs casing —
# the misc-157 profile table, made manifest data. These are platform-agnostic
# (discipline is a behavior of the server binary, not the host it ran on) and
# carry what the daemon needs to run the full diagnostics adapter: the publisher
# discipline (pull/event/debounce/scan/diff), declared constants (the debounce
# window), the capability casing (withhold the pull capability, force/forbid
# init options, declare push), and the message-compression line-strip rules.
#
#   [discipline.<server>]
#   discipline = "event"                  # pull | event | debounce | scan | diff
#   debounce_ms = 300                     # declared window for a debounce server
#   suppress_pull = true                  # withhold textDocument.diagnostic
#   declares_push = true                  # contractual publish per didOpen
#   forbidden_init_options = ["..."]      # keys enforced absent post-merge
#   forced_init_options = { key = false } # options overlaid, winning over user
#   compress_versions = ["1."]            # version prefixes the strip rules pin
#   [[discipline.<server>.compress]]      # a line-strip rule (whole-line delete)
#   prefix = "for further information visit"
#
# The compression engine (src/filter) has fixed, narrow semantics: a message
# LINE whose trimmed text matches a rule is deleted whole; a message left empty
# after stripping drops entirely; nothing is rewritten or injected; an
# out-of-pin version is pass-through. The mandate is compression, never verdict
# policy — the strip+preserve conformance fixtures assert both directions
# against the pin, so a rule that ate verdict text would fail the gate before it
# shipped (misc 165's ruling). A server absent from this table carries the empty
# discipline: no casing, no compression. An UNVERIFIED custom def (absent from
# the blessed set entirely) is enrichment-only and never a diagnostics source.

# rust-analyzer — push-first for the Rust family (RA #18709): the native pushes
# are the sole diagnostic channel, so the pull capability is withheld. The
# clippy/rustc boilerplate strip rules (formerly hand-coded in
# src/filter/rust_analyzer.rs) are pinned to the 1.x line.
[discipline.rust-analyzer]
discipline = "event"
suppress_pull = true
compress_versions = ["1."]

# Clippy "for further information visit https://..." URL lines.
[[discipline.rust-analyzer.compress]]
prefix = "for further information visit"

# Rustc/clippy lint attribution: "`#[warn(...)]` on by default" etc.
[[discipline.rust-analyzer.compress]]
prefix = "`#["
contains_any = ["on by default", "implied by", "to override"]

# Standalone `-W` flag attribution: "`-W clippy::...` implied by ...".
[[discipline.rust-analyzer.compress]]
prefix = "`-W "
contains_all = ["implied by"]

# Standalone override instruction: "to override `-W ...` add `#[allow(...)]`".
[[discipline.rust-analyzer.compress]]
prefix = "to override"
contains_any = ["`#[allow(", "`#[warn("]

# gopls — PULL discipline (diagnostics-debt 05, re-enabling bug 87's suppression).
# Bug 87 forced `pullDiagnostics` FALSE because in pull mode gopls stopped real
# pushes and published empty placeholders that the then-unconditional heard-empty
# rule (misc 153) read as authoritative — so the pull that would fetch the real
# results was suppressed and dirty files read `[clean]`. Ledger 03's version-echo
# settlement retired that defeat STRUCTURALLY: an unversioned empty placeholder
# echoes no version, so it settles nothing — the debt stays open and the pull
# (`textDocument/diagnostic`) is what settles it, pull-first. So the suppression
# is lifted (no `pullDiagnostics = false`) and gopls rides its native pull mode,
# conformance-gated against the pin (`conformance_gopls_pull_mode`).
# `diagnosticsDelay` stays enforced ABSENT (run 9): at "0s" the coupling between
# analysis completion and publishing breaks, so only gopls's own default may apply.
[discipline.gopls]
discipline = "pull"
forbidden_init_options = ["diagnosticsDelay"]

# lattice — declared push (misc 187): the contract, pinned cross-repo in misc
# 153 / Lattice ticket 16 / its decision 022 and re-verified per re-pin, is a
# publish on EVERY didOpen (unchanged files included), explicit `[]` for clean.
# The retrieval evidence bar arms on this declaration from turn zero.
[discipline.lattice]
discipline = "event"
declares_push = true

# typescript-language-server — DEBOUNCE discipline (diagnostics-debt 05). ts-ls
# is a push-only publisher (no textDocument/diagnostic pull) whose first
# didOpen/didSave diagnostic lands on an internal debounce timer AFTER the process
# has gone scheduler-idle — the exact settle/publish gap that made it
# `conformance = false` (tui-rework 13). Ledger 05's declared-constant gate closes
# it: the ledger AWAITS the version echo bounded by this declared constant, never
# interpreting silence. The window is the DESIGN's pinned range upper bound
# (300–800 ms + 50 ms); `debounce_ms = 850` is the constant the gate awaits, data
# riding the pin and re-verified at every re-pin. The un-exemption rides this gate
# (`conformance_typescript_ls`).
[discipline.typescript-language-server]
discipline = "debounce"
debounce_ms = 850

