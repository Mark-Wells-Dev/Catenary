# Default language classification, root marker rules, and server bindings.
#
# Loaded as the lowest-priority config layer. User and project configs
# override individual fields via field-level merge. Server bindings
# reference canonical names from `defaults/servers.toml` — languages
# without a `servers` entry work for classification but have no LSP
# intelligence until the user adds a server binding.
#
# Classification precedence: shebang > filename > extension.
#
# Root markers define project boundaries within workspace roots.
# When a file is opened, Catenary walks up from the file toward the
# workspace root, stopping at the first directory containing any marker.
# That directory becomes the server instance's root. Override with
# `root_markers = [...]` or disable with `root_markers = []`.

# ── Systems ─────────────────────────────────────────────────────────

[language.rust]
extensions = ["rs"]
servers = ["rust-analyzer"]
root_markers = ["Cargo.toml"]

[language.go]
extensions = ["go"]
servers = ["gopls"]
root_markers = ["go.mod"]

[language.c]
extensions = ["c"]
servers = ["clangd"]
root_markers = ["compile_commands.json", ".clangd"]

[language.cpp]
extensions = ["cpp", "cc", "cxx", "h", "hpp"]
servers = ["clangd"]
root_markers = ["compile_commands.json", ".clangd"]

[language.zig]
extensions = ["zig"]
servers = ["zls"]
root_markers = ["build.zig"]

[language.d]
extensions = ["d"]
root_markers = ["dub.json", "dub.sdl"]

[language.v]
extensions = ["v"]

[language.nim]
extensions = ["nim"]

# ── JVM ─────────────────────────────────────────────────────────────

# [language.java]  — no canonical default server (jdtls requires Eclipse runtime)
[language.java]
extensions = ["java"]
root_markers = ["pom.xml", "build.gradle", "build.gradle.kts"]

[language.kotlin]
extensions = ["kt", "kts"]
shebangs = ["kotlin"]
servers = ["kotlin-ls"]
root_markers = ["build.gradle", "build.gradle.kts"]

[language.scala]
extensions = ["scala", "sc"]
shebangs = ["scala"]
servers = ["metals"]
root_markers = ["build.sbt", "build.sc"]

[language.groovy]
extensions = ["groovy", "gvy"]
shebangs = ["groovy"]

[language.clojure]
extensions = ["clj", "cljs", "cljc"]

# ── .NET ────────────────────────────────────────────────────────────

[language.csharp]
extensions = ["cs"]
servers = ["csharp-ls"]
root_markers = ["*.sln", "*.csproj"]

[language.fsharp]
extensions = ["fs", "fsx", "fsi"]
servers = ["fsautocomplete"]
root_markers = ["*.sln", "*.fsproj"]

# ── Apple ───────────────────────────────────────────────────────────

[language.swift]
extensions = ["swift"]
shebangs = ["swift"]
servers = ["sourcekit-lsp"]

[language.objective-c]
extensions = ["m", "mm"]

# ── Scripting ───────────────────────────────────────────────────────

[language.python]
extensions = ["py"]
shebangs = ["python", "python3", "python2"]
servers = ["pyright"]
root_markers = ["pyrightconfig.json", "pyproject.toml", "setup.py"]

[language.ruby]
extensions = ["rb"]
filenames = ["Gemfile", "Rakefile"]
shebangs = ["ruby", "irb"]
root_markers = ["Gemfile"]

[language.perl]
extensions = ["pl", "pm"]
shebangs = ["perl"]

[language.php]
extensions = ["php"]
shebangs = ["php"]
servers = ["intelephense"]
root_markers = ["composer.json"]

[language.lua]
extensions = ["lua"]
shebangs = ["lua", "luajit"]
servers = ["lua-ls"]
root_markers = [".luarc.json", ".luarc.jsonc"]

[language.tcl]
extensions = ["tcl"]
shebangs = ["tclsh", "wish"]

[language.crystal]
extensions = ["cr"]
shebangs = ["crystal"]
root_markers = ["shard.yml"]

# ── JavaScript / TypeScript ─────────────────────────────────────────

[language.javascript]
extensions = ["js", "mjs", "cjs"]
shebangs = ["node", "nodejs"]
servers = ["typescript-ls"]
root_markers = ["package.json"]

[language.typescript]
extensions = ["ts", "mts", "cts"]
shebangs = ["deno"]
servers = ["typescript-ls"]
root_markers = ["tsconfig.json"]

[language.typescriptreact]
extensions = ["tsx"]
servers = ["typescript-ls"]
root_markers = ["tsconfig.json"]

[language.javascriptreact]
extensions = ["jsx"]
servers = ["typescript-ls"]
root_markers = ["package.json"]

# ── Functional ──────────────────────────────────────────────────────

[language.haskell]
extensions = ["hs", "lhs"]
servers = ["haskell-ls"]
root_markers = ["hie.yaml", "cabal.project", "stack.yaml"]

[language.ocaml]
extensions = ["ml", "mli"]
servers = ["ocamllsp"]
root_markers = ["dune-project"]

[language.elm]
extensions = ["elm"]
servers = ["elm-ls"]
root_markers = ["elm.json"]

[language.gleam]
extensions = ["gleam"]
root_markers = ["gleam.toml"]

[language.elixir]
extensions = ["ex", "exs"]
shebangs = ["elixir", "iex"]
root_markers = ["mix.exs"]

[language.erlang]
extensions = ["erl", "hrl"]
shebangs = ["erl"]
root_markers = ["rebar.config"]

[language.purescript]
extensions = ["purs"]

# ── Shell ───────────────────────────────────────────────────────────

[language.shellscript]
extensions = ["sh", "bash", "zsh", "ebuild", "eclass", "install"]
filenames = ["PKGBUILD"]
shebangs = ["bash", "sh", "zsh", "dash", "ksh"]
servers = ["bash-ls"]

[language.fish]
extensions = ["fish"]
shebangs = ["fish"]

[language.powershell]
extensions = ["ps1", "psm1", "psd1"]

# ── Data science ────────────────────────────────────────────────────

[language.r]
extensions = ["r", "R"]
shebangs = ["Rscript"]
servers = ["r-ls"]

[language.julia]
extensions = ["jl"]
shebangs = ["julia"]
servers = ["julia-ls"]

[language.mojo]
extensions = ["mojo"]

# ── Web frontend ────────────────────────────────────────────────────

[language.html]
extensions = ["html", "htm"]
servers = ["vscode-html"]

[language.css]
extensions = ["css"]
servers = ["vscode-css"]

[language.scss]
extensions = ["scss"]
servers = ["vscode-css"]

[language.sass]
extensions = ["sass"]

[language.less]
extensions = ["less"]
servers = ["vscode-css"]

[language.svelte]
extensions = ["svelte"]
servers = ["svelte-ls"]
root_markers = ["svelte.config.js", "svelte.config.ts"]

[language.vue]
extensions = ["vue"]

# ── Data / config ───────────────────────────────────────────────────

[language.json]
extensions = ["json", "jsonc"]
servers = ["vscode-json"]

[language.yaml]
extensions = ["yaml", "yml"]
servers = ["yaml-ls"]

[language.toml]
extensions = ["toml"]
filenames = ["Cargo.toml", "Cargo.lock"]
servers = ["taplo"]

[language.xml]
extensions = ["xml", "xsl", "xslt", "xsd"]

[language.sql]
extensions = ["sql"]
servers = ["sql-ls"]

[language.graphql]
extensions = ["graphql", "gql"]

[language.proto]
extensions = ["proto"]

# ── Markup / docs ───────────────────────────────────────────────────

[language.markdown]
extensions = ["md", "mdx"]
servers = ["lattice"]
root_markers = [".lattice.toml", ".git"]

[language.restructuredtext]
extensions = ["rst"]

[language.latex]
extensions = ["tex", "latex"]

[language.typst]
extensions = ["typ"]

# ── Infrastructure / config languages ───────────────────────────────

[language.nix]
extensions = ["nix"]
servers = ["nil"]
root_markers = ["flake.nix"]

[language.terraform]
extensions = ["tf", "tfvars"]
servers = ["terraform-ls"]

[language.cmake]
extensions = ["cmake"]
filenames = ["CMakeLists.txt"]
servers = ["cmake-ls"]

[language.dart]
extensions = ["dart"]
servers = ["dart-ls"]
root_markers = ["pubspec.yaml"]

[language.dockerfile]
extensions = ["dockerfile"]
filenames = ["Dockerfile"]
servers = ["docker-ls"]

[language.makefile]
filenames = ["Makefile", "GNUmakefile"]

[language.just]
filenames = ["Justfile", "justfile"]
