# Default language classification, root marker rules, and server bindings.
#
# Loaded as the lowest-priority config layer. User and project configs
# override individual fields via field-level merge. Server bindings
# reference canonical names from `defaults/servers.toml` — languages
# without a `servers` entry work for classification but have no LSP
# intelligence until the user adds a server binding.
#
# Classification precedence: shebang > filename > extension.
#
# Root markers define project boundaries within workspace roots.
# When a file is opened, Catenary walks up from the file toward the
# workspace root, stopping at the first directory containing any marker.
# That directory becomes the server instance's root. Override with
# `root_markers = [...]` or disable with `root_markers = []`.

# ── Systems ─────────────────────────────────────────────────────────

[lsp.language.rust]
extensions = ["rs"]
servers = ["rust-analyzer"]
root_markers = ["Cargo.toml"]

[lsp.language.go]
extensions = ["go"]
servers = ["gopls"]
root_markers = ["go.mod"]

[lsp.language.c]
extensions = ["c"]
servers = ["clangd"]
root_markers = ["compile_commands.json", ".clangd"]

[lsp.language.cpp]
extensions = ["cpp", "cc", "cxx", "h", "hpp"]
servers = ["clangd"]
root_markers = ["compile_commands.json", ".clangd"]

[lsp.language.zig]
extensions = ["zig"]
servers = ["zls"]
root_markers = ["build.zig"]

[lsp.language.d]
extensions = ["d"]
root_markers = ["dub.json", "dub.sdl"]

[lsp.language.v]
extensions = ["v"]

[lsp.language.nim]
extensions = ["nim"]

# ── JVM ─────────────────────────────────────────────────────────────

[lsp.language.java]
extensions = ["java"]
servers = ["jdtls"]
root_markers = ["pom.xml", "build.gradle", "build.gradle.kts"]

[lsp.language.kotlin]
extensions = ["kt", "kts"]
shebangs = ["kotlin"]
servers = ["kotlin-ls"]
root_markers = ["build.gradle", "build.gradle.kts"]

[lsp.language.scala]
extensions = ["scala", "sc"]
shebangs = ["scala"]
servers = ["metals"]
root_markers = ["build.sbt", "build.sc"]

[lsp.language.groovy]
extensions = ["groovy", "gvy"]
shebangs = ["groovy"]

[lsp.language.clojure]
extensions = ["clj", "cljs", "cljc"]

# ── .NET ────────────────────────────────────────────────────────────

[lsp.language.csharp]
extensions = ["cs"]
servers = ["csharp-ls"]
root_markers = ["*.sln", "*.csproj"]

[lsp.language.fsharp]
extensions = ["fs", "fsx", "fsi"]
servers = ["fsautocomplete"]
root_markers = ["*.sln", "*.fsproj"]

# ── Apple ───────────────────────────────────────────────────────────

[lsp.language.swift]
extensions = ["swift"]
shebangs = ["swift"]
servers = ["sourcekit-lsp"]

[lsp.language.objective-c]
extensions = ["m", "mm"]

# ── Scripting ───────────────────────────────────────────────────────

[lsp.language.python]
extensions = ["py"]
shebangs = ["python", "python3", "python2"]
servers = ["pyright"]
root_markers = ["pyrightconfig.json", "pyproject.toml", "setup.py"]

[lsp.language.ruby]
extensions = ["rb"]
filenames = ["Gemfile", "Rakefile"]
shebangs = ["ruby", "irb"]
servers = ["ruby-lsp"]
root_markers = ["Gemfile"]

[lsp.language.perl]
extensions = ["pl", "pm"]
shebangs = ["perl"]

[lsp.language.php]
extensions = ["php"]
shebangs = ["php"]
servers = ["intelephense"]
root_markers = ["composer.json"]

[lsp.language.lua]
extensions = ["lua"]
shebangs = ["lua", "luajit"]
servers = ["lua-ls"]
root_markers = [".luarc.json", ".luarc.jsonc"]

[lsp.language.tcl]
extensions = ["tcl"]
shebangs = ["tclsh", "wish"]

[lsp.language.crystal]
extensions = ["cr"]
shebangs = ["crystal"]
root_markers = ["shard.yml"]

# ── JavaScript / TypeScript ─────────────────────────────────────────

[lsp.language.javascript]
extensions = ["js", "mjs", "cjs"]
shebangs = ["node", "nodejs"]
servers = ["typescript-ls"]
root_markers = ["package.json"]

[lsp.language.typescript]
extensions = ["ts", "mts", "cts"]
shebangs = ["deno"]
servers = ["typescript-ls"]
root_markers = ["tsconfig.json"]

[lsp.language.typescriptreact]
extensions = ["tsx"]
servers = ["typescript-ls"]
root_markers = ["tsconfig.json"]

[lsp.language.javascriptreact]
extensions = ["jsx"]
servers = ["typescript-ls"]
root_markers = ["package.json"]

# ── Functional ──────────────────────────────────────────────────────

[lsp.language.haskell]
extensions = ["hs", "lhs"]
servers = ["haskell-ls"]
root_markers = ["hie.yaml", "cabal.project", "stack.yaml"]

[lsp.language.ocaml]
extensions = ["ml", "mli"]
servers = ["ocamllsp"]
root_markers = ["dune-project"]

[lsp.language.elm]
extensions = ["elm"]
servers = ["elm-ls"]
root_markers = ["elm.json"]

[lsp.language.gleam]
extensions = ["gleam"]
root_markers = ["gleam.toml"]

[lsp.language.elixir]
extensions = ["ex", "exs"]
shebangs = ["elixir", "iex"]
root_markers = ["mix.exs"]

[lsp.language.erlang]
extensions = ["erl", "hrl"]
shebangs = ["erl"]
root_markers = ["rebar.config"]

[lsp.language.purescript]
extensions = ["purs"]

# ── Shell ───────────────────────────────────────────────────────────

[lsp.language.shellscript]
extensions = ["sh", "bash", "zsh", "ebuild", "eclass", "install"]
filenames = ["PKGBUILD"]
shebangs = ["bash", "sh", "zsh", "dash", "ksh"]
servers = ["bash-ls"]

[lsp.language.fish]
extensions = ["fish"]
shebangs = ["fish"]

[lsp.language.powershell]
extensions = ["ps1", "psm1", "psd1"]

# ── Data science ────────────────────────────────────────────────────

[lsp.language.r]
extensions = ["r", "R"]
shebangs = ["Rscript"]
servers = ["r-ls"]

[lsp.language.julia]
extensions = ["jl"]
shebangs = ["julia"]
servers = ["julia-ls"]

[lsp.language.mojo]
extensions = ["mojo"]

# ── Web frontend ────────────────────────────────────────────────────

[lsp.language.html]
extensions = ["html", "htm"]
servers = ["vscode-html"]

[lsp.language.css]
extensions = ["css"]
servers = ["vscode-css"]

[lsp.language.scss]
extensions = ["scss"]
servers = ["vscode-css"]

[lsp.language.sass]
extensions = ["sass"]

[lsp.language.less]
extensions = ["less"]
servers = ["vscode-css"]

[lsp.language.svelte]
extensions = ["svelte"]
servers = ["svelte-ls"]
root_markers = ["svelte.config.js", "svelte.config.ts"]

[lsp.language.vue]
extensions = ["vue"]

# ── Data / config ───────────────────────────────────────────────────

[lsp.language.json]
extensions = ["json", "jsonc"]
servers = ["vscode-json"]

[lsp.language.yaml]
extensions = ["yaml", "yml"]
servers = ["yaml-ls"]

[lsp.language.toml]
extensions = ["toml"]
filenames = ["Cargo.toml", "Cargo.lock"]
servers = ["taplo"]

[lsp.language.xml]
extensions = ["xml", "xsl", "xslt", "xsd"]

[lsp.language.sql]
extensions = ["sql"]
servers = ["sql-ls"]

[lsp.language.graphql]
extensions = ["graphql", "gql"]

[lsp.language.proto]
extensions = ["proto"]

# ── Markup / docs ───────────────────────────────────────────────────

[lsp.language.markdown]
extensions = ["md", "mdx"]
servers = ["lattice"]
root_markers = [".lattice.toml", ".git"]

[lsp.language.restructuredtext]
extensions = ["rst"]

[lsp.language.latex]
extensions = ["tex", "latex"]

[lsp.language.typst]
extensions = ["typ"]

# ── Infrastructure / config languages ───────────────────────────────

[lsp.language.nix]
extensions = ["nix"]
servers = ["nil"]
root_markers = ["flake.nix"]

[lsp.language.terraform]
extensions = ["tf", "tfvars"]
servers = ["terraform-ls"]

[lsp.language.cmake]
extensions = ["cmake"]
filenames = ["CMakeLists.txt"]
servers = ["cmake-ls"]

[lsp.language.dart]
extensions = ["dart"]
servers = ["dart-ls"]
root_markers = ["pubspec.yaml"]

[lsp.language.dockerfile]
extensions = ["dockerfile"]
filenames = ["Dockerfile"]
servers = ["docker-ls"]

[lsp.language.makefile]
filenames = ["Makefile", "GNUmakefile"]

[lsp.language.just]
filenames = ["Justfile", "justfile"]
