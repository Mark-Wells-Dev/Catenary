# Conformance fixture (tui-rework 10).
# Intentional diagnostic: ruby-lsp flags the syntax error — an unterminated
# string literal that runs to end-of-file — through the shipped default config.
answer = "not terminated
